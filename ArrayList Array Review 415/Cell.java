
/**
 * boolean occupied;

 * int id;
 * List<Cell> neighbors = new ArrayList<Cell>();


 * Write a Cell method:
  
 *  public void loadNeighbors() that will load the neighbors list
 *  with all cells that are not occupied (north, east, south, west only)

 *
 * @author (your name)
 * @version (a version number or a date)
 */

import java.util.*;
public class Cell
{
    private boolean occupied;
    List<Cell> neighbors = new ArrayList<Cell>();
    private Cell[][] arr;
    
    // public void loadNeighbors(){
        // //I'm giving up
        // //I can't do this
        // //I dont know who I think I am
        // for(int r = 0; r < Cell.length-1; r++){
            // for(int c = r; c < neighbors.size(); c++){
                // if(Cell[r][c] == null){
                // }
                
            // }
        // }
    }
    
}
