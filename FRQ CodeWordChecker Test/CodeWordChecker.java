
/**
 * Write a description of class CodeWordChecker here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class CodeWordChecker implements 
{
    public boolean isValid (String str){
        if(String.length() < min || str.length() > max){
        return false;
    }
    for(int i = 0; i < str.length; i++){
        if(str.substring(i, i + not.length().equals(not)){
            return false;
        }
    }
    return true;
}
}
